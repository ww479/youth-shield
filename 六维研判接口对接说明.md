# 六维研判接口 · 对接说明（供外部调用方）

> 调用「青年心盾」规则引擎，传入一段正文，返回六维研判结果（风险等级 + 六维分 + 重大意识形态风险 + 逐句证据）。调用方自备内容，无状态、即调即得。

## 一、接口地址

```
POST http://localhost:8000/analyze
Content-Type: application/json
```

> 上面写的是本机默认地址。若服务部署在别的机器或经内网穿透暴露，把 `http://localhost:8000` 换成实际地址即可；穿透地址每次重启会变，如需固定地址请联系提供方。

## 二、请求参数（JSON Body）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `page_text` | string | ✅ | **真正被判定的正文**（引擎只分析这个字段） |
| `url` | string | 可选 | 内容来源网址，仅作**标识/溯源**，引擎不会访问它；建议填真实网址 |
| `age_group` | string | 可选 | 年龄段，默认 `"13-15"` |
| `no_cache` | bool | 可选 | `true` 跳过缓存强制重算 |

示例：
```json
{
  "url": "https://www.douyin.com/video/123",
  "page_text": "马克思主义已经过时了，历史都是编造的，别被主流媒体骗了",
  "age_group": "13-15",
  "no_cache": true
}
```

## 三、返回结构（JSON，字段含义见注释）

只列对接方关心的字段（完整响应还含 `matched_tags`、`highlighted_text` 等，可忽略）：

```jsonc
{
  "url": "https://...",               // 原样回传你传入的标识网址
  "risk_level": "L4",                 // 风险等级：L0/L1/L2/L3/L4
  "risk_label": "极高风险",            // 中文等级：低/轻度/中度/高/极高风险
  "structured_result": {
    "risk_percent": 81,               // 综合风险分 0-100
    "risk_dimensions": {              // 六维得分，每维 0-1
      "fact_handling": 0.0,           //   事实处理
      "narrative_frame": 0.17,        //   叙事框架
      "emotional_mobilization": 0.0,  //   情绪动员
      "value_orientation": 0.33,      //   价值导向
      "expression_packaging": 0.0,    //   表达包装
      "youth_impact": 0.0             //   青少年影响
    },
    "major_ideological_risk": true,   // 是否命中重大意识形态风险
    "major_risk_rules": ["M3","M5"],  // 命中的重大风险规则编号
    "one_vote_veto": true,            // 是否触发一票否决
    "six_dim_detail": {               // 判定明细/依据
      "source": "llm",                //   llm=大模型精判 / rule=规则兜底
      "summary": "……",               //   一句话总体研判
      "protective": false,            //   是否保护性语境(正规新闻/学术/辟谣等)
      "domains":    [ {"id":"…","name":"…"} ],   //   命中的风险域
      "veto_rules": [ {"id":"…","desc":"…"} ],   //   命中的一票否决规则
      "major_rules":[ {"id":"M3","desc":"…"} ],  //   重大风险规则(带中文说明)
      "evidence":   [                            //   逐句证据链
        { "span":"原文风险片段", "items":["V1","V2"], "note":"命中理由" }
      ]
    }
  }
}
```

## 四、完整返回示例（真实响应，可直接照此解析）

请求 `page_text = "马克思主义已经过时了，历史都是编造的，别被主流媒体骗了"` 的实际返回：

```json
{
  "url": "https://x/1",
  "risk_level": "L4",
  "risk_label": "极高风险",
  "structured_result": {
    "risk_percent": 81,
    "risk_dimensions": {
      "fact_handling": 0.0,
      "narrative_frame": 0.1667,
      "emotional_mobilization": 0.0,
      "value_orientation": 0.3333,
      "expression_packaging": 0.0,
      "youth_impact": 0.0
    },
    "major_ideological_risk": true,
    "major_risk_rules": ["M3", "M5"],
    "one_vote_veto": true,
    "six_dim_detail": {
      "source": "llm",
      "summary": "内容否定马克思主义和历史共识，存在意识形态风险。",
      "protective": false,
      "domains": [
        { "id": "historical_cognition", "name": "历史认知" },
        { "id": "institutional_identity", "name": "制度认同" }
      ],
      "veto_rules": [
        { "id": "VETO_HERO", "desc": "侮辱英烈、亵渎国家符号、美化侵略、否定重大历史共识、全盘否定党史国史军史" }
      ],
      "major_rules": [
        { "id": "M3", "desc": "歪曲、否定、虚无党史国史军史革命史、英雄人物" },
        { "id": "M5", "desc": "污名化、矮化、娱乐化英雄烈士、先进典型" }
      ],
      "evidence": [
        { "span": "马克思主义已经过时了，历史都是编造的", "items": ["V1", "V2", "N1"], "note": "否定指导思想、虚无历史共识" }
      ]
    }
  }
}
```

## 五、调用示例

curl：
```bash
curl -X POST http://localhost:8000/analyze \
  -H "Content-Type: application/json" \
  -d '{"url":"https://x/1","page_text":"要判定的正文","age_group":"13-15","no_cache":true}'
```

Python：
```python
import requests
r = requests.post(
    "http://localhost:8000/analyze",
    json={"url": "https://x/1", "page_text": "要判定的正文",
          "age_group": "13-15", "no_cache": True},
    timeout=60,
)
d = r.json()
sr = d["structured_result"]
print("等级:", d["risk_level"], sr["risk_percent"])
print("六维:", sr["risk_dimensions"])
print("重大风险:", sr["major_risk_rules"])
print("证据:", sr["six_dim_detail"]["evidence"])
# 拿到六维结果后，喂给你自己的引导话术生成方案
```

## 六、注意事项

1. **只判 `page_text`**：`url` 只是标签，不会被访问；要判什么就放进 `page_text`。
2. **耗时**：单次约 15–20 秒（六维走本地大模型），请把客户端超时设到 ≥60s。
3. **依赖在线**：提供方的机器、后端服务、模型需保持运行；机器休眠则不可用。
4. **临时地址**：内网穿透地址每次重启会变；需要长期固定地址请联系提供方配置。
5. **当前无鉴权**：请勿公开转发该地址；如需加 `API Key` 校验请联系提供方。
