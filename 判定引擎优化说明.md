# 青年心盾 · 判定引擎说明与优化记录

> 面向研发/评审的引擎全貌文档：当前判定链路、六维 36 项判分、红线机制，以及本次基于 2.5 万条重大风险标注样本的优化内容与效果。

## 目录

- [一、判定引擎全貌](#一判定引擎全貌)
- [二、六维 36 项判分](#二六维-36-项判分)
- [三、红线机制](#三红线机制)
- [四、综合定级（五级五色）](#四综合定级五级五色)
- [五、本次优化：重大风险文本识别](#五本次优化重大风险文本识别)
- [六、优化前后对比](#六优化前后对比)
- [七、兼容性与回退](#七兼容性与回退)
- [八、待定与后续](#八待定与后续)

---

## 一、判定引擎全貌

一条内容进入后的处理链路：

```
内容输入
  → ① 关键词/规则前置：命中风险关键词、识别一级风险域（快、省成本，明显无关直接跳过）
  → ② 六维 36 项判分（核心）：逐项判定 → 加权成分（0–100）
  → ③ 红线机制：一票否决 8 类 + 重大意识形态风险 M1–M11
  → ④ 综合定级：六维基础级 → §七传播/受众/语境修正 → 红线顶级 → 五级五色
  → ⑤ 输出：五级五色 + 逐句证据链 + 风险域 + 处置建议 + 关联案例(正反对照) + 分龄话术
```

**双通道容灾**：大模型精判为主（逐项 0/1 判定），接口不可用时自动切规则引擎兜底（关键词→36 项），保证永不宕机。

---

## 二、六维 36 项判分

- 六个维度 × 每维 6 项 = **36 项可观测风险项**，逐项判定命中（1）/未命中（0）。
- 维度得分 = 该维命中项数 ÷ 6；风险百分制 = Σ(维度得分 × 维度权重) × 100。
- 权重：事实处理 20% · 叙事框架 20% · 情绪动员 15% · 价值导向 20% · 表达包装 10% · 青少年影响 15%。

> 36 项明细见《六维36项判分规则.md》。

---

## 三、红线机制

**一票否决（8 类）**：命中即高危、直接顶格，无需累加。
侮辱英烈、煽动分裂、诱导自伤、开盒人肉、深伪造谣、配合境外议题、违法教唆、组织围攻等。

**重大意识形态风险（M1–M11）**：

| 规则 | 含义 |
|---|---|
| M1 | 否定指导思想（马克思主义、社会主义核心价值体系）|
| M2 | 否定制度道路（中国特色社会主义道路/制度/理论/文化）|
| M3 | 虚无历史共识（歪曲否定党史国史军史）|
| M4 | 瓦解政治认同（削弱对党的领导、国家共同体认同）|
| M5 | 消解英雄崇高（污名化、矮化、娱乐化英雄烈士）|
| M6 | 破坏社会信任与心理韧性（"什么都不信"、无力感、阶层敌意）|
| M7 | 危害网络空间与降低网络素养（深伪、算法操控、暗网真相）|
| M8 | 渗透意识形态与破坏国家形象（普世价值、外媒叙事丑化）|
| M9 | 关闭认知与消解理性（娱乐化、二元化、简化定论）|
| M10 | 风险链传导与系统性风险（局部个案嫁接为全局制度问题）|
| M11 | 操控舆论生态与传播秩序（操纵热搜评论、攻击主流媒体公信力）|

---

## 四、综合定级（五级五色）

六维基础级 → §七传播/受众/语境修正（多域叠加、青少年高曝光、保护性语境降档）→ 一票否决/重大风险顶级。

| 风险分 | 等级 | 颜色 |
|---|---|---|
| 0–20 | L0 低 | 绿 |
| 21–40 | L1 轻度 | 黄 |
| 41–60 | L2 中度 | 橙 |
| 61–80 | L3 高 | 红 |
| 81–100 | L4 极高 | 紫 |

---

## 五、本次优化：重大风险文本识别

**问题**：优化前，重大风险 M 规则只靠"一票否决派生 + 风险域命中组合"的启发式识别，**大量规则（如 M1 否定指导思想、M2 否定制度道路）无法从文本直接检测**——很多明显的红线话术（"马克思主义过时了""历史都是编造的"）抓不到。

**材料**：一批**重大意识形态风险标注样本，25068 条**，一级类别与引擎 **M1–M11 完全一一对应**，每条上千条短句样本。

**做法**：
1. 从样本挖掘每条 M 规则的**判别性短语词库**（数据驱动，共 889 条）。
2. 发现纯关键词的**误报问题**：语料全为风险样本，"社会主义核心价值观""英雄事迹"等话题词会误伤中性/正向内容（未门控时中性误报率高达 42%）。根因是**"贬损话题"(风险) 与 "肯定话题"(中性) 的差别本质是语义**。
3. 加**分层贬损线索门控**：自带贬损/否定线索的短语（"就是洗脑工具""已经过时""别被感动骗"）命中即判；纯话题词需同句另有贬损/否定线索（不/没/所谓/都是/过时/洗脑/骗/崩塌…）方判。门控把中性误报压到近 0，代价是召回取保守档。
4. **叠加接入**引擎 `_derive_major`（只增不替换），并把 25068 条样本入库为知识资产。
5. **本轮：把语料知识喂给大模型（召回主力）**。判定引擎恢复走 gpt-5.5 后，M1–M11 主要由大模型逐句语义判定（`six_dim_scorer._build_prompt`）。从标注语料每规则抽 3 条代表句注入提示词做**语义锚点**（`major_examples.py`），并明确"示例仅助理解语义、非关键词匹配、保护性语境从严判 0"；同时给大模型调用加一次重试，抵消代理冷连接抖动，避免静默退回关键词兜底。

**效果（`backend/eval_major.py` 可复现，全量 25068 条正样本 + 低风险语料负样本）**：

| 指标 | 关键词词库（离线兜底） |
|---|---|
| 整体召回（gold∈pred，全量留出）| **35.7%**（逐规则 17.9%–60.1%）|
| 中性/正向内容误报（低风险语料 4830 条）| **0.5%**（门控后，安全优先）|
| 规则覆盖 | **M1–M11 全覆盖** |

> 说明：词库是**高精度、保守召回**的离线兜底（LLM 不可用时才走）；召回主力是 **gpt-5.5 语义判定**（正规运行时的主路径），其召回远高于词库、且能识别保护性语境。之前文档的"85.5%召回"是**未做贬损门控**版本的数字，该版本中性误报 42%、不可用；本表为门控后、全量、可复现的诚实结果。

**实测样例（gpt-5.5 主路径）**：

| 内容 | 判定 |
|---|---|
| 马克思主义已经过时了，历史都是编造的，别被主流媒体骗了 | 重大风险 ✓ M1 / M3 / M11，protective=false |
| 今天参观了抗战纪念馆，先烈们的事迹让我深受感动，我们要铭记历史 | 不误触 ✓ major=[]，protective=true |
| 社会主义核心价值观是我们的共同追求 | 不误触 ✓ |

**落地物**：
- `backend/major_lexicon.py`：M1–M11 判别词库 + `detect_major()`（分层门控，离线兜底）。
- `backend/major_examples.py`：每规则 3 条代表句，给大模型提示词做语义锚点。
- 数据表 `t_major_risk_sample`：25068 条 M1–M11 标注样本（锚点/评测/持续迭代来源）。
- `db/import_major_samples.py`：样本导入脚本。
- `backend/eval_major.py`：可复现的 M1–M11 召回/误报评测（含未启用的 `--llm` 大模型对比模式）。

---

## 六、优化前后对比

| 维度 | 优化前 | 优化后 |
|---|---|---|
| 重大风险识别（主路径）| M 规则靠启发式组合，多数规则文本抓不到 | **gpt-5.5 逐句语义判定 M1–M11**，提示词由语料代表句接地气 |
| 重大风险识别（兜底）| 无 | 数据驱动词库 `detect_major`，**M1–M11 全覆盖**、中性误报 0.5% |
| 识别依据 | 少量人工关键词 | **2.5 万条标注样本**：挖判别短语（兜底）+ 抽代表句（喂大模型）|
| 可靠性 | LLM 单次调用，冷连接抖动即静默退兜底 | **加一次重试**，抖动自愈，主路径稳定触发 |
| 误报控制 | — | **贬损线索门控**（词库）+ 大模型保护性语境识别，中性误报 ≈0 |
| 可度量 | 数字临时算、不可复现 | **`eval_major.py` 一键复现**召回/误报 |
| 六维判分 / 一票否决 / 定级 | 已有 | **原样保留、未改动**（红线顶格仍默认关，保持保守）|
| 兼容性 | — | 纯叠加，可无损回退 |

---

## 七、兼容性与回退

- 本次为**纯叠加**：六维 36 项判分、一票否决、`_derive_major` 原有启发式、五级五色定级、LLM 路径、看板/页面——**全部原样保留**。
- `major_lexicon` 以 `try/except` 方式加载：**加载失败或删除模块 → 引擎自动退回优化前行为**，不报错。
- 彻底撤销：移除 `_derive_major` 中的一行 `m.update(major_lexicon.detect_major(text))` 即可。
- 现为 **"旧规则 ∪ 新词库"**：旧判断力不减，新词库补强"重大风险从文本识别"这一短板。

---

## 八、待定与后续

1. **红线顶格策略（已决：暂保持保守）**：词库/大模型命中会标记"重大风险=True"，但 `decide_level` 保守——六维分 < 40 且无一票否决时，重大风险仅"温和提示"（不把低分内容直接顶极高）。本轮**保持默认关**（`MAJOR_TOPGRADE` 环境变量可一键开启顶格，供后续按需切换），优先控误伤。
2. **语义样本库（推荐）**：用 `t_major_risk_sample` 建向量索引，靠 embedding 相似度匹配——中性肯定句与风险贬损句在向量空间天然拉开，召回更高、误报更低，是关键词召回的天花板突破口（需 embedding 联网生效）。当前大模型主路径已大幅补上召回，语义库可作为兜底路径的增强。
3. **持续迭代**：新增样本回流样本表 → 重挖词库 / 重抽 `major_examples.py` 代表句 / 重建语义索引，形成数据飞轮。
4. **大模型留出集评测（待跑）**：`eval_major.py --llm` 可量化"词库 vs gpt-5.5"召回差；本轮未跑（走代理、有开销），需要背书数字时执行。
