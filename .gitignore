# ══════════════════════════════════════════════════════════════
# 白名单模式：根目录默认忽略一切，只放行后端与插件两个交付物。
# 这样根目录的 .codex/.claude/材料/笔记/*.xlsx 等敏感内容不会被提交。
# ══════════════════════════════════════════════════════════════
/*
!/青少年风险预测前后端/
!/中青网插件/
!/README.md
!/.gitignore
# 放行三份对外说明文档（判定标准/机制/接口，不含可利用词表）
!/六维36项判分规则.md
!/判定引擎优化说明.md
!/六维研判接口对接说明.md
# 注意：/视频转写研判-匹配规则说明.md 含完整红线词表与触发条件，属内部资料，不入公开仓库。
# /技术亮点-*.md 中的指标待评估集出数后再对外，暂不入库。

# ── 放行目录内部仍需排除的敏感/无关文件 ──

# 密钥与环境变量（绝不提交）
**/.env
**/.env.*
!**/.env.example
**/auth.json
**/.codex/

# Python 缓存与虚拟环境
**/__pycache__/
**/*.pyc
**/*.pyo
**/.venv/
**/venv/

# 原始采集数据（Excel/数据集，体量大且属敏感原始舆情，建库用 db 里的 dump 即可）
**/*.xlsx
**/*.xls
**/*.csv
**/数据集*/
**/材料/

# 全量语料（83MB 原始爬取舆情，本仓库公开故不入库；按 db/数据导入语句.md 自行导入或另行索取）
**/db/data.sql
**/db/data/t_corpus.sql
# db/data/ 下其余 8 张小表（词库/标签/话术/案例/分类，共约 372KB）是判定必需的规则数据，
# 必须入库，否则 clone 下来判定跑不起来 —— 故此处不排除整个 db/data/ 目录。

# 笔记软件 / 编辑器配置
**/.obsidian/
**/.vscode/
**/.idea/

# 备份 / 副本 / 过时文件（保留本地，仅不纳入仓库）
**/*备份*/
**/* - 副本*
**/*-backup/
**/*_backup.html
**/index_v2.html
**/visual-ai-extension-v1-backup/
# 过时的 PostgreSQL schema（已被 db/youth_ideology_dump.sql 取代，会误导）
**/db/schema.sql

# 日志与系统文件
**/*.log
**/.DS_Store
**/Thumbs.db
